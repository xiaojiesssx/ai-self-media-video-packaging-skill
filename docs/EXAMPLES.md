# Examples

## Synthetic fixture

The synthetic fixture can be generated locally with FFmpeg and then packaged without private media:

```bash
npm run make:synthetic
npm run example:synthetic
```

The fixture exercises the public CLI and storyboard contract without shipping
private media, personal contact assets, or a real person's image.
